apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordpress-stacks.fullname" . }}
  labels:
    {{- include "wordpress-stacks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wordpress-stacks.selectorLabels" . | nindent 4 }}
