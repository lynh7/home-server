networks:
  web:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16 # Static Docker bridge network subnet
  # macvlan_net:
  #   external: true

services:
  traefik:
    image: traefik:v3.6
    mem_limit: 256m
    cpus: 0.5
    container_name: reverse-proxy
    restart: unless-stopped
    ports:
      - "80:80"     # Router forwards external 80 → host 80 → container 80
      - "443:443"   # Router forwards external 443 → host 443 → container 443
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/letsencrypt:/letsencrypt
      - traefik-data:/etc/traefik
    networks:
      - web
    logging:
      driver: "json-file"
      options:
        max-size: "2m"
        max-file: "5"
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=proxy"
      - "--entryPoints.web.address=:80"
      - "--entryPoints.websecure.address=:443"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.letsencrypt.acme.email=tthienan1710@gmail.com"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
      - "--log.level=INFO"

    labels:
      - "traefik.enable=true"
      
      ################################################################################
      # HTTP CONFIGURATION
      ################################################################################
      - "traefik.http.routers.wordpress-dev01-http.rule=Host(`wp.tks-dev01.tlta.online`)"
      - "traefik.http.routers.wordpress-dev01-http.entrypoints=web"
      - "traefik.http.routers.wordpress-dev01-http.service=wordpress-dev01-http"

      - "traefik.http.routers.wordpress-prod01-http.rule=Host(`wp.tlta.online`)"
      - "traefik.http.routers.wordpress-prod01-http.entrypoints=web"
      - "traefik.http.routers.wordpress-prod01-http.service=wordpress-prod01-http"
      
      ################################################################################
      # SERVICE - LOAD BALANCER CONFIGURATION
      ################################################################################
      - "traefik.http.services.wordpress-dev01-http.loadbalancer.server.url=http://192.168.10.40"
      - "traefik.http.services.wordpress-prod01-http.loadbalancer.server.url=http://192.168.10.40"

      ################################################################################
      # HTTPS CONFIGURATION - MinIO
      ################################################################################
      - "traefik.http.routers.minio-dev01-https.rule=Host(`minio.tks-cluster.dev01.tlta.online`)"
      - "traefik.http.routers.minio-dev01-https.entrypoints=websecure"
      - "traefik.http.routers.minio-dev01-https.tls=true"
      - "traefik.http.routers.minio-dev01-https.tls.certresolver=letsencrypt"
      - "traefik.http.routers.minio-dev01-https.service=minio-dev01-https"
      - "traefik.http.routers.minio-dev01-https.middlewares=minio-headers"
      
      # HTTP to HTTPS redirect for MinIO
      - "traefik.http.routers.minio-dev01-http.rule=Host(`minio.tks-cluster.dev01.tlta.online`)"
      - "traefik.http.routers.minio-dev01-http.entrypoints=web"
      - "traefik.http.routers.minio-dev01-http.middlewares=minio-redirect-https"
      - "traefik.http.routers.minio-dev01-http.service=minio-dev01-https"

      ################################################################################
      # MIDDLEWARE CONFIGURATION - MinIO Headers
      ################################################################################
      # Redirect middleware
      - "traefik.http.middlewares.minio-redirect-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.minio-redirect-https.redirectscheme.permanent=true"
      
      # Headers middleware for MinIO
      - "traefik.http.middlewares.minio-headers.headers.customrequestheaders.X-Real-IP="
      - "traefik.http.middlewares.minio-headers.headers.customrequestheaders.X-Forwarded-For="
      - "traefik.http.middlewares.minio-headers.headers.customrequestheaders.X-Forwarded-Proto=https"
      - "traefik.http.middlewares.minio-headers.headers.customrequestheaders.X-Forwarded-Host=minio.tks-cluster.dev01.tlta.online"
      
      ################################################################################
      # SERVICE - LOAD BALANCER CONFIGURATION - MinIO
      ################################################################################
      - "traefik.http.services.minio-dev01-https.loadbalancer.server.url=http://192.168.10.40"
      - "traefik.http.services.minio-dev01-https.loadbalancer.passhostheader=true"

  nginx-failover:
    image: nginx:alpine
    mem_limit: 50m
    cpus: 0.25
    container_name: nginx-failover
    restart: unless-stopped
    # labels:
    #   - "traefik.enable=true"
    #   - "--providers.docker.network=web"
    #   - "traefik.http.routers.nginx-failover.rule=Host(`tlta.online`)"
    #   - "traefik.http.routers.nginx-failover.entrypoints=websecure"
    #   - "traefik.http.routers.nginx-failover.tls=true"
    ports:
      - "8081:80"
    volumes:
      - ./nginx:/usr/share/nginx/html:ro
    networks:
      - web

  coredns:
    image: coredns/coredns:latest
    network_mode: "host"
    ports:
      - "192.168.1.100:53:53/udp"
      - "192.168.1.100:53:53/tcp"
    restart: unless-stopped
    command: -conf /etc/coredns/Corefile

  # pihole:
  #     image: pihole/pihole:2025.07.1
  #     mem_limit: 256m
  #     cpus: 0.5
  #     container_name: pihole
  #     hostname: pihole
  #     restart: unless-stopped
  #     networks:
  #       macvlan_net:
  #         ipv4_address: 192.168.1.98
  #     ports:
  #       # DNS Ports
  #       - "53:53/tcp"
  #       - "53:53/udp"
  #       # Default HTTP Port
  #       - "80:80/tcp"
  #       # Default HTTPs Port. FTL will generate a self-signed certificate
  #       - "443:443/tcp"
  #       # Uncomment the below if using Pi-hole as your DHCP Server
  #       - "67:67/udp"
  #     environment:
  #       TZ: "VN"
  #       FTLCONF_webserver_api_password: ${PIHOLE_PASSWORD}
  #       DNSMASQ_LISTENING: "all"
  #       FTLCONF_dns_upstreams: '1.1.1.1;1.0.0.1;2606:4700:4700::1111;2606:4700:4700::1001' #cloudflare DNS
  #       #FTLCONF_dns_upstreams: '9.9.9.9;149.112.112.112' #Quad9 DNS
  #     volumes:
  #       - pihole-data:/etc/pihole
  #     cap_add:
  #       - NET_ADMIN
  #     healthcheck:
  #       test: ["CMD", "dig", "@127.0.0.1", "google.com"]
  #       interval: 1m
  #       timeout: 10s
  #       retries: 3
  #     logging:
  #       driver: "json-file"
  #       options:
  #         max-size: "10m"
  #         max-file: "3"
        
volumes:
  # pihole-data:
  #   external: true # Use volume
  traefik-data:
    name: traefik-data