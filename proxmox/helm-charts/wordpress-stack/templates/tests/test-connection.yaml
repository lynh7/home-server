apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordpress-stacks.fullname" . }}-test-connection"
  labels:
    {{- include "wordpress-stacks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordpress-stacks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
